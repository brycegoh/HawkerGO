<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hawkergo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HawkerGO">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.android.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".ReviewSubmissionActivity"
            android:parentActivityName=".MainActivity"
            android:label="@string/reviewSubmitTitle">

        </activity>

        <activity
            android:name=".activities.AddHawkerStall"
            android:exported="false" />

        <activity
            android:name=".HawkerCentreActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".HawkerStallActivity"
            android:parentActivityName=".HawkerCentreActivity" />

    </application>

</manifest>